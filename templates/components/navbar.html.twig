<nav class="bg-gray-900 p-5">
    <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8">
        <div class="relative flex items-center justify-between">
            <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-start">
                <a class="flex-shrink-0 flex items-center" href="https://skript-mc.fr">
                    <i class="fa fa-arrow-left text-white mr-3"></i>
                    <img class="h-8 w-auto" src="https://skript-mc.fr/assets/images/logo-white.png" alt="Workflow">
                </a>
            </div>
            <div class="absolute inset-y-0 right-0 flex items-center pr-2 sm:static sm:inset-auto sm:ml-6 sm:pr-0">
                {# Parsing tips #}
                <div class="ml-3 relative">
                    <button class="bg-blue-300 text-gray-900 p-1 rounded-full h-8 w-16 flex items-center justify-center">
                        <span class="sr-only">View notifications</span>
                        <span class="mr-2 text-bold">0</span>
                        <i class="fa fa-lightbulb"></i>
                    </button>
                </div>

                {# Parsing warnings #}
                <div class="ml-3 relative">
                    <button class="bg-yellow-300 text-gray-900 p-1 rounded-full h-8 w-16 flex items-center justify-center">
                        <span class="sr-only">View notifications</span>
                        <span class="mr-2 text-bold">0</span>
                        <i class="fa fa-exclamation"></i>
                    </button>
                </div>

                {# Parsing errors #}
                <div class="ml-3 relative">
                    <button class="bg-red-400 text-gray-900 p-1 rounded-full h-8 w-16 flex items-center justify-center">
                        <span class="sr-only">View notifications</span>
                        <span class="mr-2 text-bold">0</span>
                        <i class="fa fa-times"></i>
                    </button>
                </div>
            </div>
        </div>
    </div>
</nav>
{{ encore_entry_script_tags('fontawesome') }}